$("#updatedpost_<%= @post.id %>").modal('hide');
//run a function after the fade out effect is completed.
$("#post_<%= @post.id %>").fadeOut(500, function(){
	//remove and prepend with new values
	$(this).remove();
	$("#container_posts").prepend('<%= j render @post %>');
});